'use client';
import { useEffect, useRef, useState } from 'react';
import { denormaliseBbox } from '@/lib/bbox';
import type { Verdict } from '@/lib/types';
const strokes: Record<string, string> = { pass: '#10b981', fail: '#ef4444', warn: '#f59e0b', manual_review: '#7c3aed', na: '#6b7280' };
export function AnnotatedImage({ imageSrc, verdicts }: { imageSrc: string; verdicts: Verdict[] }) { const imageRef = useRef<HTMLImageElement>(null); const [size, setSize] = useState({ w: 0, h: 0 }); useEffect(() => { const update = () => { if (imageRef.current) setSize({ w: imageRef.current.clientWidth, h: imageRef.current.clientHeight }); }; update(); window.addEventListener('resize', update); return () => window.removeEventListener('resize', update); }, []); return <div className="relative inline-block"><img ref={imageRef} src={imageSrc} alt="Scanned label" className="h-auto max-w-full" onLoad={() => imageRef.current && setSize({ w: imageRef.current.clientWidth, h: imageRef.current.clientHeight })} />{size.w > 0 && <svg className="pointer-events-none absolute inset-0" width={size.w} height={size.h} viewBox={`0 0 ${size.w} ${size.h}`}>{verdicts.flatMap((verdict) => verdict.evidence_bboxes.map((bbox, index) => { const box = denormaliseBbox(bbox, size.w, size.h); return <rect key={`${verdict.rule_id}-${index}`} x={box.x} y={box.y} width={box.w} height={box.h} fill="none" stroke={strokes[verdict.status] ?? '#000'} strokeWidth={2} />; }))}</svg>}</div>; }
