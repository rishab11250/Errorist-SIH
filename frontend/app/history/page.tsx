'use client';
import Link from 'next/link'; import { useEffect, useState } from 'react'; import { getHistory } from '@/lib/api'; import { ScanHistoryTable, type HistoryRow } from '@/components/ScanHistoryTable';
export default function HistoryPage() { const [rows, setRows] = useState<HistoryRow[] | null>(null); const [error, setError] = useState<string | null>(null); useEffect(() => { getHistory(50).then(setRows).catch((reason) => setError(reason instanceof Error ? reason.message : 'unknown')); }, []); return <main className="mx-auto max-w-5xl space-y-4 p-6"><div className="flex items-center justify-between"><h1 className="text-2xl font-bold">Scan history</h1><div className="space-x-3 text-sm"><Link href="/" className="text-blue-600 hover:underline">New scan</Link><Link href="/dashboard" className="text-blue-600 hover:underline">Dashboard</Link></div></div>{error && <div className="text-red-700">{error}</div>}{!rows && !error && <p>Loading…</p>}{rows && <ScanHistoryTable rows={rows} />}</main>; }
