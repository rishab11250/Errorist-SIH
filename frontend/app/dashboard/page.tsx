'use client';
import Link from 'next/link'; import { useEffect, useState } from 'react'; import { DashboardCards, type DashboardData } from '@/components/DashboardCards'; import { getDashboard } from '@/lib/api';
export default function DashboardPage() { const [data, setData] = useState<DashboardData | null>(null); const [error, setError] = useState<string | null>(null); useEffect(() => { getDashboard().then(setData).catch((reason) => setError(reason instanceof Error ? reason.message : 'unknown')); }, []); return <main className="mx-auto max-w-5xl space-y-6 p-6"><div className="flex items-center justify-between"><h1 className="text-2xl font-bold">Dashboard</h1><div className="space-x-3 text-sm"><Link href="/" className="text-blue-600 hover:underline">New scan</Link><Link href="/history" className="text-blue-600 hover:underline">History</Link></div></div>{error && <div className="text-red-700">{error}</div>}{!data && !error && <p>Loading…</p>}{data && <><DashboardCards data={data} /><div><h2 className="mb-2 text-lg font-semibold">Recent activity</h2>{data.recent_activity.length === 0 ? <p className="text-slate-500">No scans yet.</p> : <ul className="space-y-1 text-sm">{data.recent_activity.map((row) => <li key={row.scan_id}><Link href={`/scan/${row.scan_id}`} className="text-blue-600 hover:underline">Scan #{row.scan_id}</Link>{' — '}{row.overall_status} @ {new Date(row.created_at).toLocaleString()}</li>)}</ul>}</div></>}</main>; }
